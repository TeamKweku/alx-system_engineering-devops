# Puppet manifest that fixes and extension error `phpp` to `php` in a `wp-settings.php`

exec { 'fix-wordpress':
  command => 'sed -i s/phpp/php/g /var/www/html/wp-settings.php',
  path    => '/usr/local/bin/:/bin/'
}
